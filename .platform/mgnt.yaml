global:
  logging:
    level: error
fullnameOverride: "argocd"
configs:
  cm:
    controller.diff.server.side: "true"
    url: http://argocd.sbp.local
    application.resourceTrackingMethod: annotation+label
  params:
    server.insecure: true
server:
  extraArgs:
    - --insecure
